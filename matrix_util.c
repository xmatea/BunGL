#include "matrix_util.h"

/*double matrix_rand_interval(double min, double max) {
	return 0;
  double d = 0;
  d = (double) rand() / ((double) MATRIX_RAND_MAX + 1);
  return (min + d * (max - min));
}*/